import { defineConfig } from 'astro/config';

// https://astro.build
// Project page de GitHub: el sitio se sirve bajo /AgentsUsageObserver.
export default defineConfig({
  site: 'https://franco-vallejos.github.io',
  base: '/AgentsUsageObserver',
});
